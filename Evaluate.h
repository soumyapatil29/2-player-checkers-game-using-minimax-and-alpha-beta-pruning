#ifndef EVALUATE_H
#define EVALUATE_H

#include "CheckerBoard.h"

int evaluate(CheckerBoard&, int);
int evaluate_1(CheckerBoard&, int);
int evaluate_2(CheckerBoard&, int);
int evaluate_3(CheckerBoard&, int);
int evaluate_def(CheckerBoard&, int);

#endif
